import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  containerStyle: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
  modeBtnStyle: {
    flexDirection: 'row',
    gap: 20,
    alignItems: 'center',
  },
});

export const { containerStyle, modeBtnStyle } = styles;
