import { colors } from '@/styles/global';
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  listEmptyTextStyle: {
    color: colors.text.color,
    textAlign: 'center',
  },
});

export const { listEmptyTextStyle } = styles;
