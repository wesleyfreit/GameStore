import { StyleSheet } from 'react-native';
import { colors } from '../../../styles/global';

const styles = StyleSheet.create({
  titleModalStyle: {
    fontSize: 20,
    fontWeight: '600',
    color: colors.text.color,
  },
});

export const { titleModalStyle } = styles;
