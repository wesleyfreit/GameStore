import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  tabBarIconStyle: {
    width: 30,
    height: 30,
    borderWidth: 2,
    borderRadius: 30,
  },
});

export const { tabBarIconStyle } = styles;
